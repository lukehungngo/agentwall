version: "3.8"
services:
  chromadb:
    image: chromadb/chroma:latest
    ports:
      - "0.0.0.0:8000:8000"
    environment:
      - ALLOW_RESET=true
  qdrant:
    image: qdrant/qdrant:latest
    ports:
      - "0.0.0.0:6333:6333"
